# Sources

Human-readable source views and import notes. The private canonical registry belongs at
`99_System/sources.yaml`; the public shape is `99_System/sources.example.yaml`.

Sources may be accessed by a native connected account, signed-in browser, official API, watched
folder, provider export, or manual import. Each collection creates a receipt and preserves the
authoritative source reference.

