# Channel Connections Cases

This catalog owns the shared Telegram, Slack, and WhatsApp setup lifecycle. Transport-specific
delivery remains owned by the channel's runtime QA and cannot be replaced by setup/API tests.

## Natural User Use Case Checklist

| ID | Natural user action | Real surface | Supporting evidence | Expected visible result | Forbidden result | Last run | Run notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `CHANNEL-001` | Finish Easy Install, open Settings > Channels, and view all channels without terminal setup. | Installer CLI and headed browser | API state, runtime readiness, core-chat answer | Core chat is already usable; Telegram, Slack, and WhatsApp show their prerequisites and current readiness. | A terminal secret prompt blocks first chat, or an unconnected channel says Ready. | PARTIAL | Production-built browser and installer contracts passed; signed clean-Mac bootstrap remains externally blocked. |
| `CHANNEL-002` | Connect Telegram with a synthetic valid token, pair a synthetic user, exchange two turns, refresh, restart, test, then disconnect. | Settings > Channels and Telegram | Encrypted DB shape, gateway/conversation state, API, secret scan, worker/process evidence | One encrypted admin-owned record and one user mapping persist; both turns use one Viventium conversation; no token appears in browser storage, generated env, logs, or API responses. | Plaintext token, duplicate poller, direct-model path, lost/duplicate reply, fake Connected state, or core chat outage. | PARTIAL | Headed pairing/hash/privacy passed; dedicated external Telegram delivery credentials were unavailable. |
| `CHANNEL-003` | Enter a malformed/revoked Telegram token, repair it, tamper with each stored credential-envelope segment, then simulate an operator-owned or competing poller. | Settings > Channels and Telegram | Synthetic provider fixture, authenticated-ciphertext tests, scoped worker evidence, UI copy | Invalid/auth/conflict states are distinct; IV/ciphertext/tag tampering and the unreleased legacy format require reconnect without starting a worker; repair is transactional; Viventium never kills or replaces an unknown poller. | Generic success, unauthenticated credentials trusted or migrated, working credential overwritten by a failed repair, raw provider error, broad process kill, or duplicate updates. | PARTIAL | Authenticated-envelope, conflict, repair, and headed failure-recovery regressions passed; revoked-token provider proof was not run. |
| `CHANNEL-004` | Download the Slack manifest, create a synthetic Socket Mode app, enter app/bot tokens, pair, exchange threaded and direct messages, restart, remove a scope, repair, and disconnect. | Settings > Channels and synthetic Slack workspace | Manifest snapshot, auth/socket state, gateway/conversation state, DB/API, secret scan | Manifest and least scopes are explicit; encrypted connection and mappings persist; DM and thread behavior is correct; missing scope is actionable; no signing secret is requested. | Token in URL/storage/log, duplicate/lost reply, wrong thread, direct-model path, or setup failure blocks core chat. | PARTIAL | Manifest, browser secret handling, transport/thread/recovery automation passed; no dedicated external workspace. |
| `CHANNEL-005` | Configure WhatsApp Business Cloud with localhost, then enter a stable public Viventium HTTPS origin in Settings; complete Meta callback verification, pair, exchange messages, restart, repair, and disconnect. | Settings > Channels and synthetic Meta Business app | Origin validation, generated callback, HMAC/callback/webhook state, queue/idempotency evidence, DB/API, secret scan | Local/private origins are rejected, including mapped IPv6, unspecified/site-local addresses, and trailing-dot localhost/local names; the validated origin produces the secret callback path; verified HTTPS changes action-required to Connected; webhook returns quickly; duplicate deliveries produce one reply; WABA and phone scope match. | Consumer WhatsApp automation, Settings claiming to provision the public edge, any private/local origin shown Connected, unverified webhook shown Connected, forged/cross-tenant payload, credential leak, duplicate reply, or core chat outage. | PARTIAL | Settings-owned origin, official Cloud API UI, canonical HTTPS validation, HMAC/staged-verification/idempotency automation passed; no dedicated Meta app/public callback authority was available. |
| `CHANNEL-006` | Use Custom Settings Install with the existing Telegram Keychain reference, then try to enable browser Telegram concurrently. | Custom Settings compiler, operator bridge, Settings > Channels | Generated Telegram service env, ownership state, public-safety scan | The operator path keeps its consumed `0600` service env; browser Telegram fails closed with a clear ownership action. Slack/WhatsApp remain browser-owned only. | Browser secret compiled to env, duplicate poller, operator token exposed, or browser flow silently replaces the operator bridge. | PARTIAL | Compiler and ownership/conflict regressions passed; real Keychain state was intentionally not accessed. |
| `CHANNEL-007` | Corrupt one optional channel config or stop its worker, then start/restart/upgrade Viventium and retry an interrupted delivery. | Helper/CLI, browser chat, channel worker | Runtime health, queue/dedupe state, visible answer, channel status/log class, persistence | The channel is degraded/action-required while core browser chat remains usable; a retry resumes or produces exactly one durable reply; an ambiguous provider send becomes terminal and visibly tells the user to check the conversation before retrying. | Optional channel failure blocks install/first answer/restart/upgrade/restore, acknowledged work is silently lost, or two workers report the same uncertain transition. | PARTIAL | Durable retry/recovery, degraded startup, atomic uncertain-transition, visible warning, and upgrade automation passed; external delivered-message proof was unavailable. |
| `CHANNEL-008` | Sign in as two synthetic local users; each creates a one-use code and pairs a different external identity. Try using, stealing, reusing, and expiring each code. | Two browser sessions and synthetic provider identities | Authenticated API principal, hashed code state, mapping rows, routed user ID, conversation/memory isolation | Each code binds only to the signed-in user who created it, works once, expires, and never grants another user's agent/memory/tool context. | Admin bearer code shared with users, caller-selected target user, cross-user mapping, reusable code, plaintext stored code, or unpaired access. | PARTIAL | Headed admin/regular-user boundary plus hashed one-use code and cross-user automation passed; no two external provider identities. |
| `CHANNEL-009` | Start two API processes, connect a channel, kill the worker owner mid-turn, restart it, then disconnect through the other process. | Multi-process API, channel lease, durable ingress/outbox | Lease owner/expiry, queue transitions, provider acknowledgement, one delivered reply, final worker count | Exactly one active provider worker owns the account; takeover occurs after lease expiry; durable work completes once; disconnect converges across processes. | Duplicate pollers/sockets, orphan worker, acknowledged lost message, duplicate reply, or process-local Connected state. | PARTIAL | Lease/fencing/takeover/durable-delivery automation passed; real two-process external provider delivery was not run. |
| `CHANNEL-010` | Disconnect or replace a channel while another API process is restoring, testing, or reporting provider health from its old worker; then reconnect the disconnected channel. | Channel lifecycle and worker runtime | Generation CAS, stored ciphertext/state, test and health generations, start/stop events | The newer configuration wins; stale credentials are never re-saved; a stale failed test or late health event cannot demote or stop a healthy worker; reconnect succeeds without database repair; any stale worker that started is stopped. | Restored old ciphertext, old-generation health/test state overwriting a newer result, a stale cleanup stopping the replacement worker, reconnect conflict after an intentional disconnect, or an orphan worker using old credentials. | PASS | Cross-service restore/disconnect, stale Test Connection, late provider-health and cleanup generation fencing, reconnect, and successful-start race regressions passed. |
| `CHANNEL-011` | Leave an Agent turn or provider delivery in a permanent failure state. | Durable delivery queue | Attempt count, terminal queue state, scrubbed payload, partition progress | Automatic retries stop at the configured bound, payload/reply are scrubbed, and unrelated conversations keep moving. | Seven-day paid-turn loop, unbounded provider retry, or one poisoned conversation blocking every conversation for that user. | PASS | Agent failure, provider rejection, and per-conversation partition regressions passed. |
| `CHANNEL-012` | Redeliver the same Slack event with a different Socket Mode envelope ID, then send malformed input without a stable provider ID. | Slack transport and durable dedupe | Normalized envelope, dedupe hash, acknowledgement/enqueue calls | Stable Slack event/message IDs dedupe retries; malformed input is acknowledged without enqueue. | Delivery-envelope ID creates a second Agent reply, or blank IDs collapse unrelated events. | PASS | Stable-event and missing-identity regressions passed. |
| `CHANNEL-013` | Start while channel-owned Mongo indexes are temporarily unavailable, then retry after recovery. | Channel persistence readiness | Initializer and timer call counts, concurrent callers, startup/periodic error logs | The 30-second reconciliation timer exists before startup readiness; concurrent callers share one attempt; a rejection can self-heal without restart. | One transient startup fault disables Channels until process restart, or reconciliation errors disappear from logs. | PASS | Timer-before-readiness, retry-after-rejection, error logging, and success-cache regressions passed. |
| `CHANNEL-014` | Complete an Agent turn with files/status but no text, retry the provider event, evict the generation job, and replay a legacy terminal-empty row. | Gateway stream, ingress ledger, and channel client | AgentController count, terminal SSE, conversation store, persisted outcome | The turn is terminal exactly once, conversation continuity is saved, and the channel shows a truthful open-Viventium fallback. | Re-running the paid Agent turn, treating an error as success, or losing completion after job eviction/upgrade. | PASS | Live, persisted, legacy, client fallback, and SSE error regressions passed. |
| `CHANNEL-015` | Deliver a paired channel turn in Native Easy Install while no TCP API listener exists, then remove or stale the private API socket and restore it. | Real local Unix HTTP server, channel gateway client, and 30-second worker reconciler | POST chat and GET SSE requests, signed headers, socket ownership/mode, failure class, timer retry | Both requests use the private Unix socket and existing signed Agent gateway; missing/stale socket reports `connection_unavailable` and self-heals after repair. | Loopback fallback, direct Agent/model call, secret/signature loss, cached socket failure, or workers remaining disabled until process restart. | PASS | Real local Unix-server POST/SSE, missing/stale classification, same-adapter recovery, and timer recovery regressions passed. |
| `CHANNEL-016` | Receive Markdown, long Unicode, media, or a generated file through the browser-owned Telegram connection. | Telegram transport and signed Agent gateway stream | Provider request bodies, chunk reconstruction, Agent call count, SSE attachment event | Supported text renders safely and reconstructs exactly without splitting astral characters; invalid formatting retries once as plain text; unsupported inbound media and generated files receive an explicit open-in-Viventium path. | Broken Unicode or surrogate pair, raw Markdown artifacts, silent media/file loss, repeated fallback sends, or submitting an unsupported media caption as if the file were processed. | PASS | Formatter/transport/gateway regressions passed, including nested formatting, readable tables, HTML fallback, code-point-safe chunking, eight Bot API media classes, and both explicit-event and terminal-response Agent file disclosure. Full Telegram media transfer remains intentionally unsupported on this text-only connection. |
| `CHANNEL-017` | Replace a connected WhatsApp configuration that still needs Meta webhook verification, test the active connection, then send an outbound message. | Settings cache and WhatsApp Cloud transport | Mutation cache summary and Graph API request URL | The UI remains pending/action-required after connect and test instead of reverting to the active summary; every Graph request includes the correct HMAC `appsecret_proof` without exposing either secret. | False Connected state, lost pending callback/instructions, missing/incorrect proof, or access token/app secret in the URL. | PASS | Pending replacement service/cache regressions and exact outbound proof regression passed with synthetic values. External Meta callback acceptance remains covered by `CHANNEL-005` and is still PARTIAL. |
| `CHANNEL-018` | Flood one external identity, race first-window pairing guesses, send unsupported Telegram media while unpaired, or remap a queued paired user before Agent work. | Durable ingress quota, pairing-attempt store, and delivery queue | Atomic counters, account capacity, admission order, Agent call count, terminal queue update | One identity cannot drain shared capacity; at most the configured guesses enter a window; unsupported media is admitted and authorized before any fixed reply; remap is detected before paid Agent work and gets one recovery send with no retry. | Shared-account starvation, first-window counter reset, unpaired/quota-bypassing media reply, private answer for the prior user, paid Agent work after remap, or repeating a recovery message after provider rejection. | PASS | Queue/quota/pairing regressions passed, including 100-way concurrent floods, first-window attempts, durable unsupported-media admission, and pre-Agent remap checks. |
