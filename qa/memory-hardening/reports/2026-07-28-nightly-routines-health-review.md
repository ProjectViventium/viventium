# 2026-07-28 Nightly Routines Health Review

<!-- qa-evidence-exempt: Recurring read-only nightly routine audit with timing-window table; standard V2 report template remains required for feature implementation reruns. -->

Read-only overnight-routine audit from the public checkout. No model-backed apply, manual
transcript ingest, catch-up, rebuild, repair, scheduler start/stop, Docker stop, or owner data
mutation was triggered by this audit. Raw prompts, transcript text, private schedule bodies, local
paths, account identifiers, launch tokens, callback payloads, and browser snapshots are omitted.

## Timing Anchor

- Current local time sampled at audit start: 2026-07-28 11:15:55 EDT.
- Current UTC time sampled at audit start: 2026-07-28T15:15:55Z.
- Final report time: 2026-07-28 11:29 EDT / 2026-07-28T15:29Z.
- System timezone: America/Toronto.
- Generated runtime timezone: America/Toronto.
- Observer automation fire observed by this audit: about 2026-07-28 11:15 EDT /
  2026-07-28T15:15Z.
- Automation prompt still describes the observer RRULE as 11:15Z. In this Desktop environment the
  observed fire was around 15:15Z / 11:15 EDT; this is an observer-cadence label mismatch, not a
  product scheduler failure.

| Routine | Due local | Due UTC | Completion grace used | Status at audit | Observed run evidence |
| --- | --- | --- | --- | --- | --- |
| Memory hardening LaunchAgent | 2026-07-28 03:00 EDT | 2026-07-28T07:00:00Z | 60 minutes, to 04:00 EDT / 08:00Z | Due and completed | Receipt fired 03:00:00 EDT, finished 03:01:53 EDT, exit 0 |
| Transcript ingest / summary / RAG maintenance | 2026-07-28 03:00 EDT | 2026-07-28T07:00:00Z | Same as hardener | Due and completed as healthy no-op | Same hardener run: 39 files seen, 33 unchanged, 0 pending, 0 vector errors |
| Prompt Workbench nightly reflection | 2026-07-28 03:00 EDT | 2026-07-28T07:00:00Z | 60 minutes, to 04:00 EDT / 08:00Z | Due and completed | Scheduled run started 03:00:17 EDT, completed 03:03:30 EDT |
| Periphery risk-radar artifact | Generated by Workbench run | Generated by Workbench run | Must be fresh and non-stale | Fresh artifact, degraded source snapshot | Artifact generated 03:01 EDT; UI showed source snapshot missing Mongo-backed evidence |
| User-level scheduler rows | Varies | Varies | Separate user schedule policy | NOT DUE / out of built-in nightly scope | Scheduler showed 0 active due-now rows at audit time; one visible later row was before its due time |
| Status bar / manual transcript ingest | No nightly due window | No nightly due window | Not applicable | No changed manual-ingest state observed | Helper was running; audit did not trigger manual ingest |

## Verdict

Built-in nightly scheduler lanes are PASS, with one periphery source-coverage degradation.

- Memory hardening: PASS.
- Transcript ingest/catch-up/RAG maintenance: PASS as a healthy scheduled no-op.
- Prompt Workbench scheduled prompt chain: PASS.
- Periphery risk-radar artifact: PASS for fresh typed artifact; PARTIAL/DEGRADED for source
  coverage because today's snapshot missed Mongo-backed conversations/memories/messages.
- Scheduler and GlassHive substrate: PASS/WATCH.
- Power budget: PASS, no skip recorded.
- User-level schedules: NOT DUE / out of built-in nightly scope.
- Status aggregation and sidecar ownership: WATCH.
- Claude second opinion: BLOCKED by local Claude CLI unavailability/hang.

## Evidence Checked

Memory hardening:

- Active LaunchAgent was loaded in the user GUI domain, not running after completion, with one
  `StartCalendarInterval` at Hour 3 / Minute 0, no `StartInterval`, direct wrapper execution, and
  last exit code 0.
- Generated runtime env agreed on `VIVENTIUM_MEMORY_HARDENING_ENABLED=true`, `0 3 * * *`,
  America/Toronto, OpenAI, `gpt-5.6-sol`, and `xhigh`.
- Dedicated hardener status reported healthy state, `missed_expected_window=false`,
  complete execution tuple, and no provider/model/effort mismatch.
- Latest scheduled receipt recorded trigger source `launchd`, fired at 2026-07-28T07:00:00Z,
  finished at 2026-07-28T07:01:53Z, exit code 0, and requested/effective OpenAI
  `gpt-5.6-sol` / `xhigh`.
- Redacted run log recorded one eligible/applied user hash, one model attempt, zero model failures,
  zero fallback, complete 7-day lookback, 268 messages and 52 conversations fed, and input below
  the 500k cap.
- Changed memory section names were `world`, `context`, and `signals`; raw memory values were not
  inspected or copied.

Transcript/RAG:

- Hardener transcript telemetry: 39 files seen, 6 ignored sidecars, 33 unchanged, 0 pending,
  0 cap skips, 0 summary failures, 0 requeued missing vectors, 0 vector presence errors, and
  0 chars fed to the transcript model.
- RAG `/health` returned `UP`; RAG API and vector DB containers were healthy.
- Browser transcript recall was not rerun, so no new browser recall UX signoff is claimed here.

Workbench/Scheduler/GlassHive:

- Scheduling Cortex `/health` was ok with isolated runtime identity and public-safe DB identity.
- Built-in Workbench definition was active, daily 03:00 America/Toronto, memory mode off, task
  executor `glasshive_host`, channel `workbench`, next run 2026-07-29T07:00:00Z, last status
  success/sent.
- Today's scheduled prompt row had rendered hash, variable snapshot hash, GlassHive run ref,
  callback payload, private detail pointer, status completed, and no error class.
- GlassHive runtime DB showed today's run completed with no failure class. The host worker was
  `codex-cli` on `gpt-5.6-sol`, state ready, and today's `run.queued`, `run.started`, and
  `run.completed` callbacks all delivered once.
- Callback outbox had 0 pending and 0 delivering callbacks. Historical dead letters and one old
  queued `host_worker_busy` run remain as watch items, but no fresh backlog affected today's run.
- Playwright opened the active Workbench sidecar and visibly confirmed the built-in schedule
  enabled, next Jul 29 03:00, daily 03:00 America/Toronto, execution through GlassHive host,
  `codex-cli`, model `gpt-5.6-sol`, requested effort `xhigh`, Jul 28 run completed, and private
  details stored in the run detail file.

Periphery artifact and snapshot:

- Workbench API and browser UI showed 11 indexed periphery artifacts: 3 passed, 7 warning, 1 failed.
- The newest `risk_radar` artifact was fresh and passed quality: generated Jul 28 03:01 EDT,
  confidence `medium_high`, severity `low_medium`, 10/10 source refs resolved, markdown present,
  stale after Aug 4 03:01 EDT, and zero memory proposal refs.
- The same Workbench UI showed today's evidence snapshot as degraded with Mongo listed as a missing
  prerequisite and zero conversations, memories, or messages included. Current read-only checks
  showed Mongo reachable on the expected local port and the process had started before the 03:00
  window, so this is classified as a periphery snapshot collector/config/path degradation, not a
  current database-down outage.
- The artifact generation was honest and non-stale, so the scheduled Workbench chain is not failed.
  The degraded source scope remains PARTIAL until a later run proves Mongo-backed periphery
  evidence is included again.

Power and surrounding services:

- Power source was AC with the internal battery charged; today's hardener did not record a power or
  thermal skip. Live thermal proof is limited because no separate thermal sample completed.
- Firecrawl root returned 200, but `/health` and `/v1/health` returned 404. Keep this as a
  health-probe/observability watch item.
- Broad status still has needs-attention/account-action signals and points Prompt Workbench at
  8781 while active Workbench state points to 8782. Both 8781 and 8782 answered `/api/health`.
  Keep duplicate sidecar/status ownership as a watch item.
- Git status was already heavily dirty with many unrelated modified and untracked files before this
  report. This audit did not touch runtime code and did not rely on unrelated source drift to judge
  live product health.

## Commands Run

- `uv run --with pytest --with pyyaml --with pydantic --with fastapi --with croniter python -m pytest tests/release/test_default_nightly_routines.py tests/release/test_memory_hardening_contract.py tests/release/test_config_compiler.py tests/release/test_prompt_workbench.py tests/release/test_periphery_eval_harness.py tests/release/test_scheduling_mcp_supervision.py tests/release/test_rag_api_override_contract.py tests/release/test_continuity_audit.py -q`
  - Result: 341 passed, 23 skipped in 57.29s.
- `node qa/meeting-transcript-memory/evals/run-evals.cjs`
  - Result: 12 passed, 0 failed.
- `qa/periphery-nightly-insights/scripts/run-periphery-evals.py`
  - Result: 6 passed, 0 failed; dry-run only, including the degraded-Mongo case.
- `bin/viventium memory-dedupe --dry-run --json`
  - Result: zero duplicate memory entry groups/docs, zero duplicate key groups/docs, zero deletes,
    indexes not created.
- Playwright CLI against the active Workbench sidecar.
  - Result: visible Workbench schedule/run/artifact state matched Scheduler and GlassHive evidence.
  - Private-content Playwright snapshot files created during this audit were deleted.

## Second Opinion

`ClaudeViv` and `claudeviv` were not installed as commands. Local Claude CLI fallback was attempted
twice in review-only mode with a sanitized packet and tools disabled. The first attempt emitted a
tool-list warning and then stayed silent; the second clean attempt also stayed silent until stopped.
No independent Claude review was completed, so this remains an explicit review gap.

## Next Actions

1. Investigate why the 03:00 periphery evidence snapshot reported Mongo missing even though Mongo
   was reachable at audit time and the process started before the due window.
2. Reconcile duplicate Prompt Workbench sidecar/status ownership so status, helper, and active
   Workbench agree on one port.
3. Fix or document the Firecrawl health endpoint contract.
4. Retire or reconcile historical GlassHive dead letters and the old queued host-worker-busy row.
5. Retry Claude review after local Claude is responsive if a release-grade second opinion is
   required.
