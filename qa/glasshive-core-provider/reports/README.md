# GlassHive Core Provider QA Reports

Store dated, public-safe reports here. Each report must list every applicable `GCP-` case as `PASS`,
`FAIL`, `PARTIAL`, or `BLOCKED`, state exactly which real surfaces were run, and link supporting
source/config/log/DB/runtime evidence without copying secrets, personal data, private prompts, or
machine-local paths.

The current recovery evidence is `2026-08-06-telegram-reconciliation-fallback-recovery.md`; retain
older reports as dated evidence rather than rewriting their historical outcomes.
