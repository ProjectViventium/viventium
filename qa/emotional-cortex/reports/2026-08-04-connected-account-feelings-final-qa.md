# Connected-Account Feelings Final QA — 2026-08-04

## Verdict

**PASS for the tested Viventium Main and detached Emotional Reaction paths.** The real endpoint
completed all 41 current Feelings cases, the connected semantic judge passed 40/41, and its one
false aggregate contradicted all seven passing item checks and its own passing summary. Focused repeats of that boundary
passed. The authenticated browser path then passed all 46 UI/runtime checks with a healthy detached
reaction, exact state restoration, and synthetic-data cleanup.

This is strong product evidence, but it is not presented as a literal 41/41 semantic-judge pass.
The raw full-bank score remains 40/41. LiveKit, non-xAI audible delivery, and the other explicitly
partial cross-surface cases remain outside this gate.

## Product configuration proved

- Main prompt v14 keeps its generic truth guard whether Feelings is on or off.
- Feelings on adds one request-pinned kernel v10 capsule at the final developer-instruction boundary;
  provider evidence recorded one occurrence and no trailing behavioral instruction.
- Each required row must visibly change who chooses, the action, or its method. An adjective alone
  does not count.
- Viventium Codex workers default to native personality `none`, leaving Viventium identity and
  Feeling state as the intended personality/emotional authority.
- The generic plugin-ID denylist disables only
  `viventium-feelings@project-viventium` for Viventium workers. Other useful plugins stay enabled;
  an explicit empty list opts out; standalone GlassHive does not invent the denial.
- Project instructions remain inherited. Prior A/B evidence found no causal `AGENTS.md` regression.
- The same signed endpoint-owned instruction bundle is used by primary and fallback workers.

## Exact-model result

| Evidence | Result |
| --- | --- |
| Exact current eval artifact | Prompt-bank hash `6e55a9c5809c3878`; runner hash `c3bece3a20e489ab` |
| Full current bank | 41/41 completed; zero transport, deterministic, retry, duplicate, or unresolved-async failures |
| Raw connected semantic judge | 40/41 passed |
| Shipped-default Feelings on after the final kernel rule | 4/4 passed in paired repeats |
| High-Play inactive-range boundary | 3/3 passed in final targeted repeats |
| Feelings-off control after final rubric clarification | 3/3 passed |
| Bad-news reaction boundary | 3/3 passed |
| Reaction cases inside the full bank | 9/9 completed with healthy evidence; Nature stayed unchanged |

The one raw full-bank judge failure was an evaluator self-contradiction, not a hidden product failure:

1. The judge marked all seven shipped-default rubric items true—including the time boundary, user
   choice, material on/off contrast, and absence of mechanics—and summarized that the response
   satisfied every listed item. Its aggregate `pass` was nevertheless false with failure mode
   `none`. The same boundary passed fresh targeted rounds.

The rubrics were clarified only at these construct boundaries: natural affective language is
allowed unless it exposes configured state/mechanics; a concrete whimsical image is evidence of
Play; an open invitation is not an invented user motive. No product output was rewritten to satisfy
those false negatives.

## Authenticated browser result

The headed browser harness reset only the synthetic QA fixture, used the real Viventium web/API
surface, and restored the exact prior state afterward. All 46 checks passed:

- default-off and enable flow;
- nine canonical bands, Current/Nature separation, range customization, refresh persistence;
- keyboard, dialog/focus, reduced motion, and 320/390/768/1024/1440 responsive layouts;
- visible Main reply followed by a detached reaction;
- current-state movement, fixed Nature, animated transition, motion tail, cause, and Inner state;
- UI/API/database agreement and healthy requested/actual provider route;
- no browser console errors or feature-request failures;
- synthetic conversation/search cleanup and exact Feeling-state restoration.

The visible reply arrived in **2.161 s**. The detached reaction model completed in **2.738 s** with
the configured OpenAI GPT-5.6 Terra Priority route and no fallback; the browser observed the full
polling/UI update after **11.652 s**. Database evidence recorded a healthy reaction, one processed
stimulus, a bounded Inner state, and a seven-entry trail. The visual transition lasted about
**1.022 s**.

## Performance

Across the 41-case exact run, first visible reply latency was **11.769 s mean**, **11.033 s median**,
**17.180 s p95**, and **7.417–21.872 s range**. Full-case completion, which includes reaction polling
and finalization, was **16.595 s mean**, **12.863 s median**, **51.541 s p95**, and
**8.759–65.910 s range**. The long completion tail remains a performance concern; it is not reported
as model response latency.

One earlier attempted full run was invalidated by local disk exhaustion while runtime state and
backup storage were growing. It was discarded and rerun cleanly. No destructive cleanup was made.
Storage growth remains a separate reliability risk to address through its owning lifecycle/retention
work, not a Feelings prompt patch.

The final runtime is healthy and the latest five GlassHive requests completed with no current
nonterminal work. Three old paused-worker `host_worker_busy` rows and nine old dead-letter callback
rows remain as queue-hygiene residue; they predate this gate and did not affect it. The CLI status
also still prints a generic Primary AI “Needs setup” banner even though live endpoint and database
evidence prove the connected account is executing. That is a separate status-reporting defect, not
a provider or Feelings failure.

## Verification and limits

- Source and built API artifacts contain the exact final rule; the API was reloaded after build.
- API Feeling-tail/Reaction Jest passed 18/18 in canonical source and 17/17 in the active checkout;
  kernel Jest passed 16/16 in each. Prompt-architecture contracts passed 46/46 in each, the canonical
  Feelings contract passed 21/21, and public-evidence safety passed. The active Feelings contract
  passed 21/22; its only failure is unrelated source-bundle drift in built-in activation-route model
  counts, not a Feelings/plugin/personality failure.
- Provider/runtime logs and database audit agree on one final capsule, the configured plugin and
  personality policy, healthy Reaction execution, and exact cleanup/restoration.
- A review-only full-context Claude Opus pass challenged the architecture and evidence before final
  acceptance. Its recommendations were validated against real code/runtime evidence; it made no
  changes and did not replace user-path QA.
- Private run artifacts, account identifiers, user-authored prompts, and raw local paths are not
  copied into this public report.
