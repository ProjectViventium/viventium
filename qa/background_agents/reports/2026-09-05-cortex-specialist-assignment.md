# ACT-46: specialist assignment

## Summary

Status: **PARTIAL**. The invocation correction is loaded in the local development candidate.
The original full-action browser acceptance remains open.

## Scope Run

`BackgroundCortexService.executeCortexOnce` retains full conversation evidence and appends the
source-owned `cortex.execution_subject` assignment. The compiled prompt registry supplies its text;
there is no inline fallback. Main, compact internal tasks, tools, and configured models are unchanged.
The old invocation made Main's raw request the native worker's current actionable task, despite
correct retrieval instructions. Two earlier real runs performed that task inside Deep Memory.

## Automated Evidence

- Two causal missing-assignment failures against the old implementation; eight focused integrated
  service checks pass with the correction, including preserved tool-message links and compact tasks.
- The same three Workbench cases ran before and after on the same synthetic owner and saved source:
  agreed launch scope was retrieved correctly; generic inline HTML and supplied-number requests
  produced silent Deep Memory results. Main delivered the requested replies in both arms.
- Actual native receipts show `codex-cli / gpt-5.6-sol / medium`. Both recall runs called the existing
  authorized `file_search` and received the normal saved conversation. The proposed specialist input
  contains the registered assignment as its current task; the old input and Main inputs do not.
- The configured direct primary, `openAI / gpt-5.6-terra / medium`, failed authentication. The
  automatic semantic judge also failed authentication. Neither gate is recorded as passed.
- Byte-exact inputs, source/runtime hashes, run IDs, storage receipts, and visible native tool
  evidence are retained privately with the local implementation evidence.

## Findings

Exercise the original request to create, verify, and open two deliveries in parallel in the real
app. Confirm the authorized workers own the work, Deep Memory stays within retrieval, and the user
can continue chatting. Inspect visible cards and persisted/reloaded results. The small paired cases
do not prove this full-action path or native-package/channel parity.

Keep the separate Strategy/Background Analysis evidence-quality gap open: a proposed-arm Analysis
result claimed no recorded scope despite a retrievable agreement. Main and Deep Memory were correct.
This report is not a blanket cognition pass.

## Traceability

Background specialist assignment -> cortex execution scope -> ACT-46 -> retrieve the admitted
conversation without taking Main's task -> paired source/native receipts -> full-action browser
acceptance remains open as described above.

## Full-View Evidence Checklist

Paired source and native evidence supports only the listed scope. Full-action cards, detail state,
reload, and installed channel parity remain open.

## User-Grade Evidence

- Surface exercised: local development Main and GlassHive paired evaluation; full browser acceptance remains open.
- Real user path: the requested two parallel deliveries and continued conversation remain unproved.
- Visible outcome: paired Main replies were delivered; full requested delivery is not certified.
- Expanded/detail state: worker cards and detailed full-action state remain unverified.
- Persistence/reload result: full-action persistence and reload remain unverified.
- Backend/log/DB confirmation: private native receipts support the tool and assignment evidence above.
- Final model/runtime wording check: the paired evidence supports retrieval scope only; primary-route and semantic-judge authentication failed.
- Substitution check: paired evaluations and logs do not substitute for the open browser journey.

## Public-Safety Review

This report contains sanitized product behavior and model settings. Raw prompts, source/runtime
identities, native receipts, and user data remain private.
