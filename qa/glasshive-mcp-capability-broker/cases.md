# GlassHive MCP Capability Broker Cases

| Case | User Story | Requirement | Surface | Evidence | Expected Result | Last Run |
| --- | --- | --- | --- | --- | --- | --- |
| GH-MCP-BROKER-001 | A signed-in user launches a GlassHive workspace and expects the worker to use connected MCPs itself. | Broker MCP projected through `bootstrap_bundle_json`; host chat model does not choose Google/MS365 tools. | LibreChat -> GlassHive MCP -> worker workspace | Tool-call args, `.mcp.json`, AGENTS/CLAUDE, route logs | Worker workspace has one `glasshive-user-capabilities` MCP with a short-lived broker grant and compact context note. | 2026-05-27 PARTIAL: unit/API/path tests pass; no authenticated end-user launch was run. |
| GH-MCP-BROKER-002 | A second user must not see or invoke the first user's connected MCPs. | Multi-user isolation and grant user binding. | Broker MCP route | Broker grant tests, route logs, DB/token scope inspection | Cross-user grant/list/invoke attempts are rejected. | 2026-05-27 PASS/PARTIAL: live primary-vs-QA account probe showed Google+MS365 for the primary account, MS365-only for QA, no QA Google tools, tampered/expired grants rejected, and DB token rows stayed user-scoped. No two-human browser account handoff was needed or used. |
| GH-MCP-BROKER-003 | A user revokes or refreshes Google/MS365 OAuth while a worker is running. | Current auth state checked on list/describe/invoke. | Broker MCP route + LibreChat MCP token store | OAuth token mocks or local synthetic provider logs | Next broker call reports auth-required or uses refreshed token; no stale provider token is exposed. | 2026-05-27 PASS/PARTIAL: live broker discovery refreshed access tokens through LibreChat token resolvers and reported QA Google as `server_unavailable` with zero tools; real upstream revoke cycle not run. |
| GH-MCP-BROKER-004 | A worker tries to read mailbox/file content or mutate a calendar/email item. | Content-read and write policies are separate from metadata reads. | Broker MCP tool call | Structured broker response and audit log | Content reads require host-signed grant content-read scope; worker self-asserted intent flags do not authorize reads; writes require signed host confirmation; unconfirmed calls fail closed. | 2026-05-27 PASS: worker self-confirmation is blocked; signed host confirmation requires invocation id and strips broker metadata before provider call. |
| GH-MCP-BROKER-005 | A scheduled or low-level worker run needs connected MCP access. | Every launch/create/run/schedule path receives broker bootstrap or fails closed. | GlassHive MCP tools | Unit tests across launch paths | `workspace_launch`, `workspace_schedule`, `worker_create`, `worker_find_or_resume`, `worker_run`, and `worker_schedule` can carry broker bootstrap. | 2026-05-27 PASS/PARTIAL: runtime API/MCP tests cover host materialization and worker run/schedule bundle propagation; scheduled grants extend within a cap, but far-future/recurring renewal remains a follow-up. |
| GH-MCP-BROKER-006 | A sandbox or host worker is inspected after launch. | No provider secrets in workspace, logs, callbacks, or QA artifacts. | Filesystem/logs/browser | Secret scan, `.mcp.json` inspection, callback/log inspection | Only broker grant appears; no Google/MS365 refresh/access/client secret or API key is present. | 2026-05-27 PASS/PARTIAL: unit secret assertion, owner-only file mode tests, targeted secret scan, browser health, logs, DB scope, and privacy-minimized live provider calls inspected; no real connected-account worker workspace filesystem was launched in this pass. |
| GH-MCP-BROKER-007 | Existing background productivity agents are considered for permanent removal or release-wide replacement. | Shadow-mode parity before cutover. | Background agents + GlassHive worker | Paired eval report | Permanent retirement/removal is blocked until numeric parity, OAuth, isolation, and security gates pass. | 2026-05-27 BLOCKED for permanent retirement/removal: ACT-27 proves the requested local main-agent soft-retirement across browser, Telegram, and LiveKit, but paired broker-vs-legacy parity, scheduled grant renewal, and full security cutover evidence remain required. |
| GH-MCP-BROKER-008 | A user asks a generic inbox question such as "any new emails today?" without naming a provider. | Approved fix 1: non-important ambiguity should use best judgment/get what is available; generic inbox means all configured connected email providers. | LibreChat main prompt -> GlassHive broker path | Prompt source diff, prompt registry render, browser transcript, tool args | The assistant does not ask Gmail-vs-Outlook clarification, does not claim no mail tools are wired, and either checks all available connected email providers or names the exact provider/auth blocker. | 2026-05-27 PASS: real browser connected-inbox prompt launched GlassHive without a provider clarification and returned connected-provider results inline; private account evidence is kept outside the repo. |
| GH-MCP-BROKER-009 | A user inspects prompt changes before approving or auditing runtime behavior. | Approved fix 2: prompt edits remain visible and diffable in Prompt Workbench; MCP/tools are preferred when suitable without becoming invented worker goals. | Prompt Workbench + prompt registry | Workbench prompt detail/diff, registry tests, source prompt paths | `main.truth_live_data` and `mcp.glasshive_workers.server` show the edited source and working-tree diff in Prompt Workbench, and rendered prompt output matches the registry. | 2026-05-27 PASS/PARTIAL: source prompts and prompt-registry render tests passed; live authenticated prompt endpoint route returned the expected auth-protected error object for source-id lookup, so UI diff inspection remains a follow-up. |
| GH-MCP-BROKER-010 | A connected-account task is delegated to GlassHive. | Approved fix 3: worker bootstrap carries broker capability context and host-bound content-read scope without provider secrets. | LibreChat MCP call interception -> GlassHive worker bootstrap | Tool-call args, `bootstrap_bundle_json`, `.mcp.json`, `agents.md`/`claude.md`, secret scan | Worker instructions expose broker/MCP/tool availability as context, prefer it when suitable, do not manufacture provider-specific success criteria or artifacts, content-read scope appears only when explicitly host-authorized, and no provider tokens are copied into the workspace. | 2026-05-27 PASS: browser-launched worker received broker context, host-authorized content-read scope, minimal criteria, no invented urgency rubric, and no raw provider token in worker MCP/config/instruction files. |
| GH-MCP-BROKER-015 | A host model uses GlassHive MCP for a broad task without user-specified acceptance criteria. | GlassHive caller instructions must not turn capability availability or remembered context into invented project goals or success criteria. | GlassHive MCP tool metadata + Prompt Workbench | Tool descriptions, server instructions, prompt registry, browser launch args | `workspace_launch`/`workspace_schedule` metadata tells the caller to use minimal criteria when the user did not specify them, put broker availability in context, avoid memory-derived priorities/contact lists/urgency rubrics unless requested, and trust the worker to choose the best path; old `success_criteria as broker/tool evidence gates` language is absent. | 2026-05-27 PASS: MCP metadata and browser launch args used minimal criteria, passed the user adjective through, avoided provider/rubric/artifact invention, and omitted old broker-gate wording. |
| GH-MCP-BROKER-011 | A host-mode worker starts on macOS/local development. | Approved fix 4: broker URL matches the actual LibreChat listener. | LibreChat startup + broker bootstrap + worker stderr | Generated `.env`, listen socket, projected `.mcp.json`, worker stderr | Local LibreChat binds deterministically to `127.0.0.1`; projected host-mode broker URL points at the same listener and worker MCP startup has no `127.0.0.1`/`::1` transport failure. | 2026-05-27 PASS: local runtime restart passed doctor/health and a host-mode brokered worker completed through the projected broker MCP path. |
| GH-MCP-BROKER-012 | A worker tries to read connected-account content. | Approved fix 5: content reads are authorized by host-signed grant scope, not worker self-assertion. | Broker MCP policy route | Unit tests, route tests, broker structured responses | Content-read tools fail closed without grant content-read scope even if the worker sends an explicit-intent flag; with host-bound scope they may proceed; writes still require signed host confirmation. | 2026-05-27 PASS: Jest broker policy/auth tests passed; MCP schema now states the intent flag alone does not unlock content reads or writes. |
| GH-MCP-BROKER-013 | A normal worker run lasts longer than the short default grant TTL. | Approved fix 6: broker access remains valid within a bounded renewable window and fails closed after the cap. | Broker grant verification + worker run | Unit tests, route tests, long-run synthetic probe | A grant expired by `exp` is accepted only inside `renewable_until`, marked as renewed, and rejected after `renewable_until`; replay cache TTL follows the renewable cap without widening server/tool/user scope. | 2026-05-27 PASS/PARTIAL: Jest renewal tests passed for bounded renewal and replay TTL; long-duration live worker renewal remains a scheduled follow-up. |
| GH-MCP-BROKER-014 | A user runs the single basic flow that escaped QA: ask for today's emails, wait, and expect the result. | Approved fix 7: end-to-end user QA gate for connected-account delegation through GlassHive. | LibreChat browser, GlassHive worker, broker MCP, DB/logs | Real browser transcript, tool args, worker workspace, broker logs, DB run status, visible final answer | The flow `any new emails today?` -> wait completes without unnecessary clarification, gives the worker broker MCP/tool context, calls broker tools or reports exact auth/provider blockers, returns the result inline, and does not auto-activate retired MS365/Google/Deep Research background cortices. | 2026-05-27 PASS: real browser flow launched GlassHive, completed, returned inline read-only results, no forced download/local-worker/config link appeared, and DB showed only the expected chat messages. |

## Approved Fix Acceptance Matrix

| Fix | Blast Radius | Success Criteria | User-Based Test Case | Automated/QA To Run |
| --- | --- | --- | --- | --- |
| 1. Generic ambiguity prompt | Main-agent live-data behavior for inbox/reply/provider-ambiguous requests. | Important actions still ask clarifying questions; non-important data lookups use best judgment or available evidence; generic inbox requests check all available connected providers or report exact blockers. | In a fresh chat, ask "any new emails today?" and confirm no Gmail-vs-Outlook clarification occurs. | Prompt registry test; browser QA case GH-MCP-BROKER-008. |
| 2. Prompt Workbench visibility and MCP/tool preference | Prompt source, rendered prompt registry, Workbench diff/history, GlassHive tool-selection guidance. | Edited prompts are visible in Prompt Workbench with working-tree diff; rendered registry matches source; GlassHive instructions prefer MCP/tools when suitable and forbid invented tool-choice success criteria. | Open Prompt Workbench, search `main.truth_live_data` and `mcp.glasshive_workers.server`, inspect diff/rendered text. | `tests/release/test_prompt_registry.py`; Prompt Workbench browser QA GH-MCP-BROKER-009 and GH-MCP-BROKER-015. |
| 3. Broker context bootstrap | LibreChat MCP interception, GlassHive bootstrap bundle, worker `agents.md`/`claude.md`, worker task instructions. | Broker context is available to the worker without provider secrets; content-read grant scope is explicit; worker criteria are not browser-first or broker-tool hard gates unless user-specified. | Launch a read-only connected-account worker and inspect the View/Steer workspace files. | LibreChat broker Jest tests; GlassHive bootstrap tests; GH-MCP-BROKER-010. |
| 4. Broker URL determinism | Local LibreChat startup, generated `.env`, host-mode workers, broker MCP reachability. | `HOST=127.0.0.1` is generated/upserted for local LibreChat; projected host broker URL matches the listener; no IPv4/IPv6 mismatch in worker stderr. | Restart local runtime and launch a brokered worker. | Launcher/source test; broker URL unit test; GH-MCP-BROKER-011. |
| 5. Grant-bound content-read scope | Broker auth, broker policy, all content-read MCP tools, write-confirmation boundary. | Worker-authored content flags alone do not authorize reads; host-bound grant scope does; writes remain host-confirmed. | Attempt a content-read call without grant scope, then with grant scope, using synthetic MCP tools. | Jest security tests; GH-MCP-BROKER-012. |
| 6. Renewable broker grants | Long-running workers, schedules, replay cache TTL, broker route auth. | Expired grants work only inside bounded `renewable_until`; route marks renewed requests; rejected after cap; scope never widens. | Run a synthetic long worker or direct route call with a grant past `exp` but within cap. | Jest auth/route tests; GH-MCP-BROKER-013. |
| 7. Basic user-flow QA gate | LibreChat UI, GlassHive launch/wait/status, broker MCP, background cortex activation, DB/log evidence. | The escaped flow works end to end or returns exact provider/auth blocker; no unnecessary clarification; no browser-first hard gate; no retired productivity cortex auto-activation. | Browser: ask "any new emails today?", ask it to wait, verify visible result and backend evidence. | Playwright/browser QA, DB/log inspection, GH-MCP-BROKER-014. |
