[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests that require a running OpenClaw Bridge server (deselect with '-m "not integration"')
    e2e: marks live E2E tests that start real OpenClaw Gateway processes
